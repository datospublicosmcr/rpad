// Configuración de la API
const CONFIG = {
  // URL base de la API
  API_URL: '/api',
  
  // Versión de la aplicación
  VERSION: '1.0.0',
  
  // Claves en localStorage
  TOKEN_KEY: 'rpad_token',
  USER_KEY: 'rpad_user'
};
